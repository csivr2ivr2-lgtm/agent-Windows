# AI Aharon Windows installer

This directory contains the Inno Setup definition and release packaging assets used to build a distributable `AI-Aharon-Setup-<version>.exe`.

The installer is designed to preserve machine data under `%ProgramData%\AgentWindowsAI` across upgrades while replacing application/runtime files safely.
